/**
 * Basis-URL des Node/Express-Backends (siehe backend/, deploy/). Es gibt in
 * diesem Projekt keine lokale Backend-Instanz zum Entwickeln — ng serve und
 * die Produktion sprechen beide dieselbe live laufende API an.
 */
export const API_BASE_URL = 'https://api.gleisbau-digital.org/api';
