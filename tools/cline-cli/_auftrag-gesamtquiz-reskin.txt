Du bist Gwen und arbeitest im Projekt-Root eines Angular/Ionic-Projekts (NgModule-basiert, standalone: false). Es gibt bereits eine fertige, funktionierende Datei src/app/modules/zusatz/gesamtquiz/pages/gesamtquiz.page.html (NICHT anfassen). Du sollst NUR das dazugehoerige SCSS ersetzen -- reines Farb-/Schrift-Reskin, keine Strukturaenderung.

Ersetze GENAU diese eine Datei mit EXAKT diesem Inhalt (nichts umformulieren, nichts kuerzen, nichts ergaenzen, bestehenden Inhalt komplett ersetzen):

=== DATEI: src/app/modules/zusatz/gesamtquiz/pages/gesamtquiz.page.scss ===
:host {
  font-family: var(--font-body);
  color: var(--sp-text);
}

ion-header ion-toolbar {
  --background: var(--sp-surface);
  --color: var(--sp-text);
  --border-color: var(--sp-border);
}

ion-title {
  font-family: var(--font-display);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.02em;
}

.toolbar-action-label {
  font-family: var(--font-mono);
  font-size: 12px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

ion-content {
  --background: var(--sp-bg);
  background-image:
    linear-gradient(var(--sp-border) 1px, transparent 1px),
    linear-gradient(90deg, var(--sp-border) 1px, transparent 1px);
  background-size: 32px 32px;
}

.page-shell {
  padding: 20px clamp(16px, 4vw, 40px) 48px;
  display: flex;
  flex-direction: column;
  gap: 20px;
  max-width: 820px;
  margin: 0 auto;
}

.hero {
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 16px;
  background: var(--sp-surface);
  border: 1px solid var(--sp-border);
  border-radius: 6px;
  padding: 20px;
}

.eyebrow {
  font-family: var(--font-mono);
  color: var(--sp-accent);
  font-weight: 400;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  margin: 0 0 8px;
  font-size: 12px;
}

.hero h1 {
  font-family: var(--font-display);
  font-weight: 700;
  text-transform: uppercase;
  margin: 0 0 10px;
  font-size: clamp(22px, 4vw, 28px);
  color: var(--sp-text);
}

.lede {
  color: var(--sp-text-muted);
  margin: 8px 0;
  font-size: 15px;
  line-height: 1.6;
  max-width: 60ch;
}

.badges {
  display: flex;
  gap: 6px;
  flex-wrap: wrap;
}

.badges ion-chip {
  --background: var(--sp-surface-2);
  border: 1px solid var(--sp-border);
  font-family: var(--font-mono);
  font-size: 11px;
  letter-spacing: 0.04em;
  color: var(--sp-text-muted);
}

.progress {
  display: grid;
  gap: 8px;
  min-width: 220px;
}

.stat {
  background: var(--sp-surface-2);
  border: 1px solid var(--sp-border);
  border-radius: 4px;
  padding: 8px 14px;
}

.stat span {
  display: block;
  font-family: var(--font-mono);
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--sp-text-muted);
}

.stat strong {
  display: block;
  margin-top: 2px;
  font-family: var(--font-mono);
  font-weight: 700;
  font-size: 16px;
  color: var(--sp-text);
}

.quiz-section {
  margin-top: 0;
}

ion-spinner {
  display: block;
  margin: 60px auto;
  --color: var(--sp-accent);
}

@media (max-width: 1024px) {
  .hero {
    flex-direction: column;
  }
}

WICHTIG: Schreibe die Datei exakt wie oben angegeben, mit allen Zeilenumbruechen. Melde am Ende kurz, welche Datei du geschrieben hast.
