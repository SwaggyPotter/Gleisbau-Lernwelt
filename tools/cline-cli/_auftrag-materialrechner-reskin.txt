Du bist Gwen und arbeitest im Projekt-Root eines Angular/Ionic-Projekts (NgModule-basiert, standalone: false). Es gibt bereits eine fertige, funktionierende Datei src/app/modules/zusatz/materialrechner/pages/materialrechner.page.html (NICHT anfassen). Du sollst NUR das dazugehoerige SCSS ersetzen -- reines Farb-/Schrift-Reskin, keine Strukturaenderung.

Ersetze GENAU diese eine Datei mit EXAKT diesem Inhalt (nichts umformulieren, nichts kuerzen, nichts ergaenzen, bestehenden Inhalt komplett ersetzen):

=== DATEI: src/app/modules/zusatz/materialrechner/pages/materialrechner.page.scss ===
:host {
  font-family: var(--font-body);
  color: var(--sp-text);
}

ion-header ion-toolbar {
  --background: var(--sp-surface);
  --color: var(--sp-text);
  --border-color: var(--sp-border);
}

ion-title {
  font-family: var(--font-display);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.02em;
}

.toolbar-action-label {
  font-family: var(--font-mono);
  font-size: 12px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

ion-content {
  --background: var(--sp-bg);
  background-image:
    linear-gradient(var(--sp-border) 1px, transparent 1px),
    linear-gradient(90deg, var(--sp-border) 1px, transparent 1px);
  background-size: 32px 32px;
}

.page-shell {
  padding: 20px clamp(16px, 4vw, 40px) 48px;
  display: flex;
  flex-direction: column;
  gap: 18px;
  max-width: 780px;
  margin: 0 auto;
  width: 100%;
}

.hero {
  background: var(--sp-surface);
  border: 1px solid var(--sp-border);
  border-radius: 6px;
  padding: 20px;
}

.eyebrow {
  font-family: var(--font-mono);
  color: var(--sp-accent);
  font-weight: 400;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  margin: 0 0 8px;
  font-size: 12px;
}

.hero h1 {
  font-family: var(--font-display);
  font-weight: 700;
  text-transform: uppercase;
  margin: 0 0 10px;
  font-size: clamp(22px, 4vw, 28px);
  color: var(--sp-text);
}

.lede {
  color: var(--sp-text-muted);
  margin: 8px 0;
  font-size: 15px;
  line-height: 1.6;
  max-width: 60ch;
}

.badges {
  display: flex;
  gap: 6px;
  flex-wrap: wrap;
}

.badges ion-chip {
  --background: var(--sp-surface-2);
  border: 1px solid var(--sp-border);
  font-family: var(--font-mono);
  font-size: 11px;
  letter-spacing: 0.04em;
  color: var(--sp-text-muted);
}

.difficulty-row {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

ion-segment {
  --background: var(--sp-surface-2);
  border: 1px solid var(--sp-border);
  border-radius: 4px;
}

ion-segment-button {
  --color: var(--sp-text-muted);
  --color-checked: var(--sp-bg);
  --indicator-color: var(--sp-accent);
  --background-checked: transparent;
  font-family: var(--font-mono);
  font-size: 11px;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  min-height: 40px;
}

.stats-row {
  display: flex;
  gap: 6px;
  flex-wrap: wrap;
}

.stats-row ion-chip {
  --background: var(--sp-surface-2);
  border: 1px solid var(--sp-border);
  font-family: var(--font-mono);
  font-size: 11px;
  letter-spacing: 0.04em;
  color: var(--sp-text-muted);
}

.stats-row ion-chip[color='primary'] {
  border-color: var(--sp-accent);
  color: var(--sp-accent);
}

.quiz-card {
  --background: var(--sp-surface);
  color: var(--sp-text);
  border: 1px solid var(--sp-border);
  border-radius: 6px;
  margin: 0;
  box-shadow: none;
}

ion-card-title {
  font-family: var(--font-display);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.02em;
  font-size: 18px;
  color: var(--sp-text) !important;
}

ion-card-subtitle {
  font-family: var(--font-mono);
  font-size: 11px;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--sp-text-muted) !important;
}

ion-list {
  background: transparent;
}

ion-item {
  --background: var(--sp-surface-2);
  --color: var(--sp-text);
  --border-radius: 4px;
  --border-color: var(--sp-border);
  --inner-border-width: 0;
  --inner-padding-end: 12px;
  font-family: var(--font-body);
  margin-bottom: 8px;
  border-radius: 4px;
  overflow: hidden;
}

ion-item.correct {
  --background: rgba(111, 191, 115, 0.16);
  --color: #bfe8c2;
  font-weight: 600;
}

ion-item.wrong {
  --background: rgba(224, 128, 111, 0.16);
  --color: #f2c2b8;
  font-weight: 600;
}

.feedback {
  margin: 12px 0;
  padding: 12px 14px;
  border-radius: 4px;
  border: 1px solid var(--sp-border);
}

.feedback strong {
  display: block;
  margin-bottom: 4px;
  font-family: var(--font-display);
  text-transform: uppercase;
  letter-spacing: 0.02em;
}

.feedback p {
  margin: 0;
  font-size: 14px;
  color: var(--sp-text);
}

.feedback.correct {
  background: rgba(111, 191, 115, 0.1);
  border-color: rgba(111, 191, 115, 0.35);
}

.feedback.correct strong {
  color: var(--sp-success);
}

.feedback.wrong {
  background: rgba(224, 128, 111, 0.1);
  border-color: rgba(224, 128, 111, 0.35);
}

.feedback.wrong strong {
  color: var(--sp-danger);
}

.nav-row {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 8px;
}

.spacer {
  flex: 1;
}

ion-button {
  --background: var(--sp-accent);
  --background-hover: #ff9954;
  --color: #0f1b24;
  --border-radius: 4px;
  font-family: var(--font-display);
  font-weight: 600;
  letter-spacing: 0.02em;
  text-transform: uppercase;
}

.table-section {
  padding-bottom: 24px;
}

.table-section ion-button {
  --background: transparent;
  --color: var(--sp-accent-2);
  --box-shadow: none;
}

.density-table {
  background: var(--sp-surface);
  border: 1px solid var(--sp-border);
  border-radius: 6px;
  padding: 12px 16px;
}

.density-table table {
  width: 100%;
  border-collapse: collapse;
  font-size: 14px;
  color: var(--sp-text);
}

.density-table th,
.density-table td {
  text-align: left;
  padding: 6px 8px;
  border-bottom: 1px solid var(--sp-border);
}

.density-table th {
  color: var(--sp-text-muted);
  font-family: var(--font-mono);
  font-weight: 600;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.table-hint {
  margin: 10px 2px 0;
  font-size: 12px;
  color: var(--sp-text-muted);
}

@media (max-width: 768px) {
  .page-shell {
    padding: 14px clamp(12px, 4vw, 40px) 32px;
    gap: 14px;
  }

  .hero {
    padding: 14px;
  }

  .lede {
    margin: 6px 0;
    font-size: 14px;
    line-height: 1.5;
  }

  .badges {
    gap: 4px;
  }
}

@media (max-width: 390px) {
  .page-shell {
    padding: 10px 10px 24px;
    gap: 12px;
  }

  .hero {
    padding: 12px;
  }
}

WICHTIG: Schreibe die Datei exakt wie oben angegeben, mit allen Zeilenumbruechen. Melde am Ende kurz, welche Datei du geschrieben hast.
